define([], function () {
    return ['$scope', '$compile', '$timeout', '$window', '$http', 'apiOfServer',
        function ($scope, $compile, $timeout, $window, $http, aos) {

        	$scope.$apply();
        }];
});